# Used only for the Capacitor (Android app) build — npm run build:mobile
# The native app has no same-origin backend, so this must be an absolute URL
# to the deployed backend (e.g. your Railway domain or www.expensico.com).
VITE_API_URL=https://your-backend.up.railway.app
